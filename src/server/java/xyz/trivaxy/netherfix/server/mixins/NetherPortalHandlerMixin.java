package xyz.trivaxy.netherfix.server.mixins;

import net.minecraft.src.game.level.NetherPortalHandler;
import org.spongepowered.asm.mixin.Mixin;

@Mixin(NetherPortalHandler.class)
public class NetherPortalHandlerMixin {
    // This is a marker so the mixin config plugin fires
}
